export { exportJson, exportText, importJson, importText } from "./handlers";

import { exportJson, exportText, importJson, importText } from "./handlers";

export const modlist = {
  importJson,
  exportJson,
  importText,
  exportText,
};
