export { exportBat, getLaunchOptions, launch, preflight } from "./handlers";

import { exportBat, getLaunchOptions, launch, preflight } from "./handlers";

export const launcher = {
  preflight,
  launch,
  getLaunchOptions,
  exportBat,
};
