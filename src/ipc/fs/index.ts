export {
  openExternal,
  openFolder,
  pickFile,
  pickFolder,
  pickInstallMod,
  pickSaveFile,
} from "./handlers";

import {
  openExternal,
  openFolder,
  pickFile,
  pickFolder,
  pickInstallMod,
  pickSaveFile,
} from "./handlers";

export const fs = {
  openFolder,
  openExternal,
  pickFolder,
  pickFile,
  pickSaveFile,
  pickInstallMod,
};
