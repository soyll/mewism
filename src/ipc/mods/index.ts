export {
  autoSort,
  checkChanges,
  disable,
  disableAll,
  enable,
  enableAll,
  getPreviewDataUrl,
  install,
  installFromDrop,
  list,
  moveDown,
  moveToTop,
  moveToBottom,
  moveUp,
  reorder,
} from "./handlers";

import {
  autoSort,
  checkChanges,
  disable,
  disableAll,
  enable,
  enableAll,
  getPreviewDataUrl,
  install,
  installFromDrop,
  list,
  moveDown,
  moveToBottom,
  moveToTop,
  moveUp,
  reorder,
} from "./handlers";

export const mods = {
  list,
  checkChanges,
  enable,
  disable,
  enableAll,
  disableAll,
  reorder,
  moveUp,
  moveDown,
  moveToTop,
  moveToBottom,
  autoSort,
  getPreviewDataUrl,
  install,
  installFromDrop,
};
